import { IPlaylistInfo } from "@/interface"

interface Props {
  info: IPlaylistInfo;
  className?: string;
}

export const PreviewPlaylist = ({
  info,
  className
}: Props) => {

  return (
    <>

    </>
  )
}